language: node_js
node_js:
    - "12.0"
install:
    - yarn
script:
    - yarn secrets
    - yarn build
    - yarn lint
    - yarn test
after_success:
    - yarn deploy
cache:
    directories:
        - node_modules
